 
  
// 刺突エフェクト
function applyPierceEffect(size, speed, color, direction) {
  console.log("刺突エフェクト", { size, speed, color, direction });
  // TODO: 刺突エフェクトの実装
}

// 殴打エフェクト
function applyImpactEffect(size, speed, color, direction) {
  console.log("殴打エフェクト", { size, speed, color, direction });
  // TODO: 殴打エフェクトの実装
}

// 盾エフェクト
function applyShieldEffect(size, speed, color, direction) {
  console.log("盾エフェクト", { size, speed, color, direction });
  // TODO: 盾エフェクトの実装
}

// 銃撃エフェクト
function applyGunshotEffect(size, speed, color, direction) {
  console.log("銃撃エフェクト", { size, speed, color, direction });
  // TODO: 銃撃エフェクトの実装
}

// 射出エフェクト
function applyProjectileEffect(size, speed, color, direction) {
  console.log("射出エフェクト", { size, speed, color, direction });
  // TODO: 射出エフェクトの実装
}

// 火花エフェクト
function applySparkEffect(size, speed, color, direction) {
  console.log("火花エフェクト", { size, speed, color, direction });
  // TODO: 火花エフェクトの実装
}

// 光線エフェクト
function applyLaserEffect(size, speed, color, direction) {
  console.log("光線エフェクト", { size, speed, color, direction });
  // TODO: 光線エフェクトの実装
}

// 爆発エフェクト
function applyExplosionEffect(size, speed, color, direction) {
  console.log("爆発エフェクト", { size, speed, color, direction });
  // TODO: 爆発エフェクトの実装
}

// 広がるエフェクト
function applyExpandEffect(size, speed, color, direction) {
  console.log("広がるエフェクト", { size, speed, color, direction });
  // TODO: 広がるエフェクトの実装
}

// 渦エフェクト
function applyVortexEffect(size, speed, color, direction) {
  console.log("渦エフェクト", { size, speed, color, direction });
  // TODO: 渦エフェクトの実装
}

// 球体エフェクト
function applySphereEffect(size, speed, color, direction) {
  console.log("球体エフェクト", { size, speed, color, direction });
  // TODO: 球体エフェクトの実装
}

// 波紋エフェクト
function applyRippleEffect(size, speed, color, direction) {
  console.log("波紋エフェクト", { size, speed, color, direction });
  // TODO: 波紋エフェクトの実装
}

// 雨エフェクト
function applyRainEffect(size, speed, color, direction) {
  console.log("雨エフェクト", { size, speed, color, direction });
  // TODO: 雨エフェクトの実装
}

// 落石エフェクト
function applyFallingRockEffect(size, speed, color, direction) {
  console.log("落石エフェクト", { size, speed, color, direction });
  // TODO: 落石エフェクトの実装
}

// 揺れエフェクト
function applyShakeEffect(size, speed, color, direction) {
  console.log("揺れエフェクト", { size, speed, color, direction });
  // TODO: 揺れエフェクトの実装
}

// 燃え上がるエフェクト
function applyFlameEffect(size, speed, color, direction) {
  console.log("燃え上がるエフェクト", { size, speed, color, direction });
  // TODO: 燃え上がるエフェクトの実装
}

// 突風エフェクト
function applyGustEffect(size, speed, color, direction) {
  console.log("突風エフェクト", { size, speed, color, direction });
  // TODO: 突風エフェクトの実装
}

// 割れ目エフェクト
function applyCrackEffect(size, speed, color, direction) {
  console.log("割れ目エフェクト", { size, speed, color, direction });
  // TODO: 割れ目エフェクトの実装
}

// 流星エフェクト
function applyMeteorEffect(size, speed, color, direction) {
  console.log("流星エフェクト", { size, speed, color, direction });
  // TODO: 流星エフェクトの実装
}

// サークルエフェクト
function applyCircleEffect(size, speed, color, direction) {
  console.log("サークルエフェクト", { size, speed, color, direction });
  // TODO: サークルエフェクトの実装
}

// 波エフェクト
function applyWaveEffect(size, speed, color, direction) {
  console.log("波エフェクト", { size, speed, color, direction });
  // TODO: 波エフェクトの実装
}

// 鏡エフェクト
function applyMirrorEffect(size, speed, color, direction) {
  console.log("鏡エフェクト", { size, speed, color, direction });
  // TODO: 鏡エフェクトの実装
}

// フラッシュエフェクト
function applyFlashEffect(size, speed, color, direction) {
  console.log("フラッシュエフェクト", { size, speed, color, direction });
  // TODO: フラッシュエフェクトの実装
}

// 煙エフェクト
function applySmokeEffect(size, speed, color, direction) {
  console.log("煙エフェクト", { size, speed, color, direction });
  // TODO: 煙エフェクトの実装
}
